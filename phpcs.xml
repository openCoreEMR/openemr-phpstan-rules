<?xml version="1.0" encoding="UTF-8"?>
<ruleset name="OpenEMR PHPStan Rules">
    <description>PHP CodeSniffer configuration for OpenEMR PHPStan Rules package</description>

    <!-- Files to check -->
    <file>src</file>

    <!-- Exclude patterns -->
    <exclude-pattern>*/vendor/*</exclude-pattern>

    <!-- Show progress -->
    <arg value="p"/>
    <!-- Show colors in output -->
    <arg name="colors"/>
    <!-- Run in parallel -->
    <arg name="parallel" value="75"/>
    <!-- Show sniff codes -->
    <arg value="s"/>

    <!-- Use PSR-12 as base standard -->
    <rule ref="PSR12"/>

    <!-- PHP version compatibility -->
    <config name="testVersion" value="8.1-"/>

    <!-- Additional rules -->
    <rule ref="Generic.Arrays.DisallowLongArraySyntax"/>
    <rule ref="Generic.CodeAnalysis.UnusedFunctionParameter">
        <!-- PHPStan rules must implement interface with $scope parameter -->
        <exclude-pattern>*/Rules/*</exclude-pattern>
    </rule>
    <rule ref="Generic.Commenting.Todo"/>
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="120"/>
            <property name="absoluteLineLimit" value="150"/>
        </properties>
        <!-- Allow longer lines in PHPStan rules for error messages -->
        <exclude-pattern>*/Rules/*</exclude-pattern>
    </rule>
    <rule ref="Generic.PHP.DeprecatedFunctions"/>
    <rule ref="Generic.PHP.ForbiddenFunctions"/>
    <rule ref="Squiz.WhiteSpace.SuperfluousWhitespace">
        <properties>
            <property name="ignoreBlankLines" value="false"/>
        </properties>
    </rule>
</ruleset>
